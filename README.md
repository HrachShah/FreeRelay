# FreeRelay
a local proxy that automatically rotates across all free AI API providers (Groq, Google AI Studio, OpenRouter free tier, etc.) with a single OpenAI-compatible endpoint. Drop-in replacement, zero cost, no config.
